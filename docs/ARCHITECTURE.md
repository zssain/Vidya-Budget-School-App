# ARCHITECTURE.md — how Vidya is put together

Do not add, rename or remove folders, crates or layers without asking.

## 1. Repository layout
```
vidya/
├── AGENTS.md, CLAUDE.md
├── docs/                      specifications, prompts, progress
├── reference/                 VidyaSchoolApp_step1.html (prototype)
├── scripts/                   doctor.sh, verify.sh, check-api-drift.mjs, check-i18n.mjs
├── .github/workflows/         ci.yml (every push), release.yml (version tags)
├── package.json               npm scripts, Vite, Vitest, ESLint, Prettier, Tauri CLI and JS API
├── vite.config.js
├── Cargo.toml                 Rust workspace root (members below)
├── rust-toolchain.toml
├── src/                       frontend (shared by desktop and mobile)
│   ├── index.html
│   ├── main-desktop.js        registers all desktop views
│   ├── main-mobile.js         registers teacher and accountant views only
│   ├── core/  api/  views/  components/  styles/  locales/
├── src-tauri/                 Tauri app crate "vidya-app"
│   ├── tauri.conf.json (+ platform-specific config files)
│   ├── capabilities/
│   ├── src/  lib.rs  main.rs  state.rs  commands/  platform/  background/
│   └── gen/                   generated by Tauri (android project, schemas)
├── crates/
│   ├── vidya-core/  vidya-db/  vidya-services/  vidya-license/  vidya-sync/
│   ├── vidya-server/  vidya-client/  vidya-backup/  vidya-export/  vidya-testkit/
└── provider/                  Provider Tool (separate Tauri app, macOS)
    ├── src/  (its own small frontend)
    └── src-tauri/
```

## 2. Layers and allowed dependencies
```
            ┌──────────────┐        ┌───────────────┐
 frontend → │ src-tauri    │        │ vidya-server  │ ← phones (HTTPS on LAN)
 (invoke)   │ commands     │        │ HTTP handlers │
            └──────┬───────┘        └───────┬───────┘
                   │  both call the SAME services │
                   ▼                              ▼
            ┌────────────────────────────────────────┐
            │ vidya-services  (authorize → validate → transaction → change log)
            └───────┬───────────────────┬────────────┘
                    ▼                   ▼
            ┌─────────────┐     ┌──────────────┐
            │ vidya-db    │     │ vidya-sync   │
            └──────┬──────┘     └──────┬───────┘
                   ▼                   ▼
            ┌────────────────────────────────────────┐
            │ vidya-core  (pure rules, no I/O)       │
            └────────────────────────────────────────┘
 vidya-license → vidya-core          vidya-backup → vidya-db
 vidya-client  → vidya-sync, vidya-services (local writes on phone)
 vidya-export  → vidya-services (reads DTOs)
 vidya-testkit → everything (dev-dependency only)
```
Rule: **one path for every action**. A fee collected on the office computer and a fee collected on a phone end in the same `FeeService::collect` call on the office computer. Nothing duplicates business rules.

## 3. Cargo features in src-tauri
| Feature | Enabled for | Pulls in |
|---|---|---|
| `desktop-server` | Windows, macOS | vidya-server, vidya-backup, vidya-export, license activation, setup wizard commands |
| `mobile-client` | Android | vidya-client |

Desktop builds use `--features desktop-server` (set as default for desktop in the build scripts). Android builds use `--no-default-features --features mobile-client`. Commands that belong to one side are compiled only with that feature, so the Android app physically cannot run setup or act as a server.

## 4. Runtime structure on the office computer
```
Tauri process
├── Window (WebView) ── invoke ──► commands ──► services ──► SQLCipher database file
├── Session store (in memory): token → Actor
├── Background tasks (tokio)
│   ├── LAN server (axum + rustls) on port 47631
│   ├── Discovery: mDNS advertise + UDP responder on 47632
│   ├── Sync live notifier (WebSocket broadcaster)
│   ├── Backup scheduler (daily local copy, Drive upload when online)
│   └── Keep-awake during school hours
└── Tray / menu bar icon (window can close, process keeps running)
```

## 5. Runtime structure on a phone
```
Tauri process (Android)
├── Window (WebView) ── invoke ──► commands ──► services (client mode) ──► local SQLCipher DB
│                                                   └── writes go to outbox
└── Sync loop (vidya-client)
    ├── discover office computer (mDNS, then UDP broadcast)
    ├── pinned HTTPS + signed requests
    ├── push outbox, pull changes, WebSocket for live updates
    └── snapshot on first sync
```
Services run in **server mode** on the office computer (writes are final) and **client mode** on phones (writes are applied locally and queued; the server may reject them, which rolls them back). The mode is a field of the service container, not separate code.

## 6. Data flow examples
**Collect fee on the office computer:** view → `commands.collectFee` → `fees::collect_fee` command → `FeeService::collect(actor, input)` → `authorize(FeesCollect)` → `core::fees::validate_payment` → transaction: next receipt number, insert receipt, change-log entry → commit → `ReceiptDto` → view shows receipt.

**Collect fee on a phone:** same until `FeeService::collect` in client mode → local transaction inserts receipt with the phone's prefix (T1-0001) and an outbox change → UI shows receipt → sync loop pushes → server runs `FeeService::apply_remote_change` → permission and validation again → accepted or rejected → phone marks acknowledged or rolls back.

## 7. Error flow
Library errors (`thiserror`) → `ServiceError { kind, message_key, params, field }` → at the boundary: `AppError` JSON for Tauri, same JSON body with HTTP status for LAN. See `API.md`.

## 8. Where each spec is implemented
| Spec | Code |
|---|---|
| DATA_MODEL.md | `crates/vidya-db/migrations/`, `crates/vidya-db/src/repo/` |
| PERMISSIONS.md | `crates/vidya-core/src/permissions.rs` (+ test that parses the doc table) |
| API.md | `src-tauri/src/commands/`, `src/api/commands.js`, `crates/vidya-server/src/routes/` |
| SYNC_PROTOCOL.md | `crates/vidya-sync`, `crates/vidya-server/src/routes/sync.rs`, `crates/vidya-client` |
| LICENSE_FORMAT.md | `crates/vidya-license` |
| BACKUP_FORMAT.md | `crates/vidya-backup` |
| UI_GUIDE.md | `src/styles/`, `src/core/`, `src/components/` |
| PLATFORMS.md | `src-tauri/src/platform/` |
