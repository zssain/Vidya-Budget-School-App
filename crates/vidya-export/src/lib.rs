//! Excel export and import, and PDF helpers. Reads DTOs from `vidya-services`.
//!
//! Contains no business rules (see `crates/AGENTS.md`).

#[cfg(test)]
mod tests {
    #[test]
    fn crate_builds() {}
}
