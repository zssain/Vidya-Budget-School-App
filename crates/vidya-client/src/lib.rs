//! Discovery client, pinned HTTPS client and the sync loop for Android
//! (local writes on the phone).
//!
//! Contains no business rules (see `crates/AGENTS.md`).

#[cfg(test)]
mod tests {
    #[test]
    fn crate_builds() {}
}
