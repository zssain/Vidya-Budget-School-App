//! Activation and reset code encode, decode, verify and sign (signing behind
//! the `signing` feature).
//!
//! Contains no database access (see `crates/AGENTS.md`).

#[cfg(test)]
mod tests {
    #[test]
    fn crate_builds() {}
}
