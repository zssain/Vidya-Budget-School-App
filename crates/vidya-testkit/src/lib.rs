//! Sample school generator, simulation harness and test fixtures.
//!
//! Dev-dependency only: never referenced by production code (see
//! `crates/AGENTS.md`).

#[cfg(test)]
mod tests {
    #[test]
    fn crate_builds() {}
}
