//! Change envelopes, merge rules and protocol DTOs for device sync.
//!
//! Contains no database access and no network code (see `crates/AGENTS.md`).

#[cfg(test)]
mod tests {
    #[test]
    fn crate_builds() {}
}
