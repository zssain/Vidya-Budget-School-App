Run build prompt `$ARGUMENTS` for Vidya.

1. Read `AGENTS.md` and every applicable nested `AGENTS.md` completely.
2. Read `docs/PROGRESS.md` and `docs/prompts/$ARGUMENTS.md` completely.
3. Check the prompt dependencies are marked done.
4. Follow the prompt tasks and repository rules in order, verify the result, and update progress.
