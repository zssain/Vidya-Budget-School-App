# Dependencies and build output
node_modules/
dist/
dist-mobile/
target/
coverage/
size-report.json

# macOS
.DS_Store

# Vidya backups
*.vidyabak
*.vidyabak.partial

# Environment and secrets
.env
.env.*
*.p12
*.pem
*.p8
*.jks
*.keystore
*.mobileprovision
provider-keys/
keystore.properties
# Signing / service keys (never commit)
vidya-licence-signing.key
*-signing.key
*.private-key
google-services.json
GoogleService-Info.plist

# Real release build config (only build-config/release.json.example is committed)
src-tauri/build-config/release.json

# Dev licence service keypair (generated on first run — prompts/P03 Step 2)
cloud/licence/.dev-keys/

# Test fixtures
.test-phone-*

# Playwright
/test-results/
/playwright-report/
/blob-report/
/playwright/.cache/

# Note: src-tauri/gen/schemas/ is intentionally NOT ignored (kept in git).

# Fidelity baselines are regenerated locally until ≤0.1% tuning lands (P9)
/tests/e2e/__screens__/
